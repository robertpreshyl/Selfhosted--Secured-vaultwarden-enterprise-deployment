# Vaultwarden Environment Configuration
# Copy this file to vaultwarden.env and fill in your values

# ============================================
# DOMAIN CONFIGURATION
# ============================================
# Your domain name (e.g., vault.example.com)
DOMAIN=https://vault.example.com:8443

# ============================================
# ADMIN TOKEN (CRITICAL - GENERATE SECURE TOKEN)
# ============================================
# Generate a secure token:
#   TOKEN=$(openssl rand -base64 48)
#   echo -n "$TOKEN" | argon2 $(openssl rand -base64 32) -e -id -t 3 -m 524288 -p 4
# 
# IMPORTANT: Store the plain token in your password manager!
# Use the Argon2 hash here:
ADMIN_TOKEN=$argon2id$v=19$m=524288,t=3,p=4$REPLACE_WITH_YOUR_ARGON2_HASH

# ============================================
# SECURITY SETTINGS
# ============================================
# Disable new user signups (recommended for production)
SIGNUPS_ALLOWED=false

# Disable password hints (security best practice)
SHOW_PASSWORD_HINT=false

# Require email verification for new accounts
SIGNUPS_VERIFY=true

# Invitation org name
INVITATION_ORG_NAME=Your Organization

# ============================================
# DATABASE SETTINGS
# ============================================
# SQLite database location (inside container)
DATABASE_URL=/data/db.sqlite3

# ============================================
# SMTP EMAIL CONFIGURATION (Optional)
# ============================================
# Uncomment and configure if you want email notifications

# SMTP_HOST=smtp.example.com
# SMTP_FROM=vaultwarden@example.com
# SMTP_FROM_NAME=Vaultwarden
# SMTP_SECURITY=starttls
# SMTP_PORT=587
# SMTP_USERNAME=your-smtp-username
# SMTP_PASSWORD=your-smtp-password

# ============================================
# PASSWORD HASHING (Argon2)
# ============================================
# Memory cost in KiB (recommended: 19456-524288)
PASSWORD_MEMORY_COST=524288

# Time cost (iterations, recommended: 2-4)
PASSWORD_ITERATIONS=3

# Parallelism factor (recommended: 1-4)
PASSWORD_PARALLELISM=4

# ============================================
# PBKDF2 ITERATIONS
# ============================================
# Client-side PBKDF2 iterations (minimum: 100000)
PASSWORD_ITERATIONS=600000

# ============================================
# YUBICO 2FA SETTINGS (Optional)
# ============================================
# YUBICO_CLIENT_ID=
# YUBICO_SECRET_KEY=
# YUBICO_SERVER=

# ============================================
# MISCELLANEOUS
# ============================================
# Websocket enabled
WEBSOCKET_ENABLED=true

# Extended logging
EXTENDED_LOGGING=true

# Log level (trace, debug, info, warn, error, off)
LOG_LEVEL=info

# Log to file
LOG_FILE=/data/vaultwarden.log

# Icon service
ICON_SERVICE=internal

# ============================================
# IMPORTANT SECURITY NOTES
# ============================================
# 1. NEVER commit this file with real values to git
# 2. Always use Argon2id hashed admin token
# 3. Disable signups after creating admin account
# 4. Enable 2FA for all admin accounts
# 5. Regularly rotate admin token (every 90 days)
# 6. Keep Vaultwarden updated to latest version
