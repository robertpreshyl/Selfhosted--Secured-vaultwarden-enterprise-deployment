# Traefik Environment Configuration
# Copy this file to .env in the same directory as docker-compose.yml

# ============================================
# CLOUDFLARE DNS-01 CHALLENGE
# ============================================
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone:DNS:Edit for all zones
CF_DNS_API_TOKEN=YOUR_CLOUDFLARE_API_TOKEN_HERE

# ============================================
# TRAEFIK DASHBOARD CREDENTIALS
# ============================================
# Generate bcrypt hash for password:
#   echo $(htpasswd -nB admin) | sed -e s/\\$/\\$\\$/g
# 
# Or use online generator: https://hostingcanada.org/htpasswd-generator/
# Choose bcrypt algorithm
TRAEFIK_DASHBOARD_AUTH=admin:YOUR_BCRYPT_HASHED_PASSWORD_HERE

# ============================================
# DOMAIN CONFIGURATION
# ============================================
# Your domains for routing
TRAEFIK_DOMAIN=traefik.example.com
VAULTWARDEN_DOMAIN=vault.example.com

# ============================================
# EMAIL FOR SSL CERTIFICATES
# ============================================
# Email for Let's Encrypt notifications
LETSENCRYPT_EMAIL=admin@example.com

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit this file with real values
# 2. Keep Cloudflare API token secure
# 3. Use strong passwords for dashboard
# 4. Rotate credentials every 90 days
# 5. Monitor Traefik logs for unauthorized access attempts
